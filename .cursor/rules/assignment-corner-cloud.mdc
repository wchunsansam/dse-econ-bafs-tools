---
description: 作業角老師成績頁以雲端為準；本機殘留不可當真檔
alwaysApply: true
---

# 作業角：老師成績頁以雲端為準

老師成績／掃描／原件列表以雲端為準，不要為了「怕弄丟本機檔」而留下畫面上看得到、雲端已沒有的檔。

## 必須

- `followCloud`／老師端合併狀態時：丟掉遠端沒有的本機 `files`、`pdfSubmissions`、老師掃描 `mcSubmissions`。
- 畫面上勾得到的原件，刪除必須找得到（查全部原件池，不要只查 `markableScriptRecs`）。
- 刪除成功只在雲端入帳之後才報成功；不可先樂觀彈成功、檔卻還在雲端。入帳用小的刪除清單，不要等整份成績檔寫完才回報。
- 老師徹回／刪掃描之後，重整或再拉雲端，畫面不得再顯示本機殘留。

## 不要

- 把沒有雲端 href、或不在遠端 id 清單裡的本機掃描當可批、可刪的真檔。
- 用「本機多留幾份比較安全」當預設。離線未上傳另案處理，不可污染已跟雲端對過的老師成績頁。
